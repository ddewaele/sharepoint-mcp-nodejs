services:
  sharepoint-mcp:
    build: .
    image: sharepoint-mcp-nodejs:latest
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - HTTP_PORT=3000
    restart: unless-stopped
